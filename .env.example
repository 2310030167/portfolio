# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM="Mohammed Eajaz Ahmed <no-reply@example.com>"

# Admin Notification Email
ADMIN_EMAIL=eajazahmed@gmail.com

# Public Application URL (used to generate action links in emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Secret key used for cryptographically signing admin action tokens
ACTION_SECRET=your-secure-random-secret-key-at-least-32-chars
